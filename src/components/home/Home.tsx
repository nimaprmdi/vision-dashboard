import Grid from "@mui/material/Grid";
import { Box, Container } from "@mui/material";

const Home = (): JSX.Element => {
    return <section className="c-home">Home</section>;
};

export default Home;
